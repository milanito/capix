{
  "name": "capix-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Capix — capability-based Node.js server framework",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "biome check .",
    "docs:dev": "vitepress dev website",
    "docs:build": "vitepress build website",
    "docs:preview": "vitepress preview website"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.0",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.5.0",
    "vitepress": "^1.6.4",
    "vitest": "^3.2.7"
  }
}
