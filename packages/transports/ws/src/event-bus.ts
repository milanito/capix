// Re-exported from capix core for backwards compatibility.
// Import directly from 'capix' in new code.
export { createEventBus } from 'capix';
export type { EventBus, EventMap, SubscribeOptions } from 'capix';
