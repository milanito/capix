// Re-exported from capix core for backwards compatibility.
// Import directly from '@capixjs/core' in new code.
export { createEventBus } from '@capixjs/core';
export type { EventBus, EventMap, SubscribeOptions } from '@capixjs/core';
